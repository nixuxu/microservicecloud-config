spring:
  profiles:
    active:
    - dev

---
spring：
  profiles: dev
  application:
    name: microservicecloud-config-nixuxu-dev

---
spring:
  profiles: test
  application:
    name: microservicecloud-config-nixuxu-test

#一定要保存为UTF-8编码
